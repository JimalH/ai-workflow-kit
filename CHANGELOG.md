# Changelog

## v0.2.0
- Move entire workflow kit under `.workflow/` (loader, base rules, workflows, roles); root now keeps only platform stubs.
- Update stubs and docs to point at `./.workflow/AI_LOADER.md` and new workflow/role paths.

## v0.1.0
- Initial publication of AI workflow kit with loader, base rules, workflows, roles, and platform stubs.
